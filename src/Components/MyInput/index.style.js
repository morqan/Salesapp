import { StyleSheet } from 'react-native';

export const inputStyles = StyleSheet.create({
  container: {
    // backgroundColor: '#fff'
  },
  input: {
    borderBottomColor: '#fff',
    borderBottomWidth: 2,
    paddingVertical: 10,
    color: '#fff'
  }

});
