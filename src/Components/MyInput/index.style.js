import { StyleSheet } from 'react-native';

export const inputStyles = StyleSheet.create({
  container: {
    // backgroundColor: '#fff'
  },
  input: {
    borderColor: '#E6E6E6',
    borderWidth: 1,
    padding: 10,
    color: '#1E1E1E',
    borderRadius: 3
  }

});
