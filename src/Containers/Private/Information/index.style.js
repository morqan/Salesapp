import { StyleSheet } from 'react-native'

export const infoStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    paddingTop: 50,
    paddingHorizontal: 100,
  },
  title: {
    fontSize: 25,
    fontWeight: '600',
  },
  tagStyle: {
    p: {
      lineHeight: 30,
      fontSize: 15,
      // color: 'red'
    },
    br: {
      opacity: 0
    }
  },
})
